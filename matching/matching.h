#ifndef MATCHING_H
#define MATCHING_H

#include "othello.h"
#include "elorating.h"

std::uint8_t set_match(
    const std::string& strategy1, const std::string& strategy2, uint8_t color);

#endif  // #ifndef MATCHING_H